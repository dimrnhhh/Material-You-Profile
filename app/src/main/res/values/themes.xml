<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.MaterialYouProfile" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.AppCompat.Dialog.Alert</item>
    </style>

    <style name="Theme.MaterialYouProfile" parent="Base.Theme.MaterialYouProfile">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="ThemeOverlay.MaterialYouProfile.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>